import {
  StyleSheet
} from 'react-native';

export const styles = StyleSheet.create({
  buttonText: {
    margin: 10,
    fontSize: 12,
    textAlign: 'center',
    color: '#ffffff',
    letterSpacing: 1,
  },
  button: {
    backgroundColor: 'rgba(0,0,0,0.9)',
    borderRadius: 4,
  },
  buttonDisable: {
    opacity: 0.4,
  }
});